{
  "mcpServers": {
    "swetrix-events": {
      "command": "npx",
      "args": ["-y", "@kieksme/swetrix-events-mcp"],
      "env": {
        "SWETRIX_API_KEY": "${user_config.api_key}",
        "SWETRIX_API_BASE_URL": "${user_config.api_base_url}"
      }
    }
  }
}
